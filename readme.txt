1) Individual folders contain the data for particular benchmark under different TAM Widths and different layers.

2) Simply use the  "run.c"  to execute the assignments of cores in tiers . Before that one must ensure that they have generated the executable (here "pwr.o") from pwr_2_new_v2.c

5) Then use the "run_schedule.c"  to execute testing schedule. Before that one must ensure that they have generated the executable (here "Schduling_efficient.o") from Schduling_efficient.c